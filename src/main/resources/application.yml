spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
  kafka:
    bootstrap-servers: 192.168.49.2:9092
  redis:
    host: localhost
    port: 6379
  batch:
    job:
      enabled: true

server:
  port: 8080
